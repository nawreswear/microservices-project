server:
  port: 8383

spring:
  application:
    name: facture-service
  datasource:
    url: jdbc:mysql://localhost:3306/salesapp_factures
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
